name: CI Workflow Belajar

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout kode dari repo
      uses: actions/checkout@v3

    - name: Setup Node.js (misalnya kita pakai JS project)
      uses: actions/setup-node@v3
      with:
        node-version: '18'

    - name: Install dependencies
      run: |
        echo "🔧 Tidak ada dependencies, tapi kita simulasikan npm install"
        mkdir test_project
        cd test_project
        npm init -y

    - name: Jalankan build sederhana
      run: |
        echo "🚧 Simulasi proses build..."
        echo "console.log('Build sukses!')" > index.js
        node index.js
